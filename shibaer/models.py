


def visit2vec(numeric_data_size, embeddings, targets ):
    """
    Build a visit2vec model -- representation of a single visit

    :param numeric_data_size: int
        the numeric (e.g. labs) data size
    :param embeddings: list
        each embedding is (name, n_items, embbedding-size)

    :return: Model
    """




